
## BUGS


## TODO

~~Don't clear the Dict on each load; update and insert only.~~

~~Use new "changed_since" API parameter for incremental load. (First load is from Posix zero.)~~

~~Handle multiple result pages!~~

Better decoding of various constraints as only some have "label" but others have text in detail.

~~Separate fetches for each constraint type (only fetch those with constraints).~~

~~UI to focus on special cases.~~
> Becomes irrelevant if only fetch "specials".

Retention: Remove anything closed for more than a month.

Hide Google Maps API key.

Maybe wait for details before sending to front end.

If available, get full history &c for specials.

## Maybe not

Rework specials parsing to use discrete types.

